{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "@proj-sakura/electron-vueuse#build": {
      "dependsOn": ["@proj-sakura/electron-eventa#build"],
      "outputs": ["dist/**"]
    }
  }
}
